<Window x:Class="TtLauncher.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:TtLauncher.Converters"
        Title="TtLauncher"
        Width="860"
        SizeToContent="Height"
        MaxHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown"
        Loaded="Window_Loaded">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:IconFallbackConverter x:Key="IconFallback"/>
        <CubicEase x:Key="EaseOut" EasingMode="EaseOut"/>
        <SolidColorBrush x:Key="WindowFillBrush" Color="#D60E0F12"/>
        <SolidColorBrush x:Key="WindowBorderBrush" Color="#30FFFFFF"/>
        <SolidColorBrush x:Key="DividerBrush" Color="#16FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B8BAC1"/>
        <SolidColorBrush x:Key="TextHint" Color="#898C94"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#8EB5FF"/>
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI, Microsoft YaHei UI"/>
        </Style>
        <Style x:Key="KeycapStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="MinWidth" Value="30"/>
            <Setter Property="BorderBrush" Value="#16FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#10FFFFFF"/>
        </Style>
        <Style x:Key="TypeTagStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Background" Value="#14FFFFFF"/>
            <Setter Property="BorderBrush" Value="#12FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Tag}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ResultsContainerStyle" TargetType="Border">
            <Setter Property="MaxHeight" Value="0"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="-8"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasItems, ElementName=ResultList}" Value="True">
                    <Setter Property="MaxHeight" Value="520"/>
                    <Setter Property="Opacity" Value="1"/>
                    <Setter Property="Visibility" Value="Visible"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform Y="0"/>
                        </Setter.Value>
                    </Setter>
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard FillBehavior="HoldEnd">
                                <DoubleAnimation Storyboard.TargetProperty="MaxHeight" From="0" To="520" Duration="0:0:0.24" EasingFunction="{StaticResource EaseOut}"/>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.18"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-8" To="0" Duration="0:0:0.24" EasingFunction="{StaticResource EaseOut}"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard FillBehavior="HoldEnd">
                                <DoubleAnimation Storyboard.TargetProperty="MaxHeight" From="520" To="0" Duration="0:0:0.16" EasingFunction="{StaticResource EaseOut}"/>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.14"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="0" To="-8" Duration="0:0:0.16" EasingFunction="{StaticResource EaseOut}"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemRoot" Margin="8,2" Padding="12,10" CornerRadius="12" Background="Transparent" BorderBrush="#00FFFFFF" BorderThickness="1">
                            <Grid>
                                <Rectangle x:Name="ItemGlass" RadiusX="12" RadiusY="12" Fill="#10FFFFFF" Opacity="0"/>
                                <Rectangle x:Name="ItemSheen" Height="14" VerticalAlignment="Top" RadiusX="12" RadiusY="12" Fill="#28FFFFFF" Opacity="0"/>
                                <Border x:Name="ItemInnerBorder" CornerRadius="12" BorderBrush="#00FFFFFF" BorderThickness="1" Margin="0.5"/>
                                <ContentPresenter/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="#08FFFFFF"/>
                                <Setter TargetName="ItemRoot" Property="BorderBrush" Value="#10FFFFFF"/>
                                <Setter TargetName="ItemGlass" Property="Opacity" Value="0.42"/>
                                <Setter TargetName="ItemSheen" Property="Opacity" Value="0.18"/>
                                <Setter TargetName="ItemInnerBorder" Property="BorderBrush" Value="#10FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="#0CFFFFFF"/>
                                <Setter TargetName="ItemRoot" Property="BorderBrush" Value="#22FFFFFF"/>
                                <Setter TargetName="ItemGlass" Property="Opacity" Value="1"/>
                                <Setter TargetName="ItemSheen" Property="Opacity" Value="0.36"/>
                                <Setter TargetName="ItemInnerBorder" Property="BorderBrush" Value="#2AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <Border x:Name="RootBorder" CornerRadius="24" Background="{StaticResource WindowFillBrush}" BorderBrush="{StaticResource WindowBorderBrush}" BorderThickness="1" ClipToBounds="True">
            <Grid>
                <Rectangle Fill="#0CFFFFFF"/>
                <Rectangle Height="84" VerticalAlignment="Top" Fill="#18FFFFFF"/>
                <Rectangle Width="240" HorizontalAlignment="Right" Opacity="0.035" IsHitTestVisible="False">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#00000000" Offset="0"/>
                            <GradientStop Color="#6F8FFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#96141518" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1">
                        <Grid Margin="18,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Path Grid.Column="0" Width="18" Height="18" Margin="0,0,14,0" VerticalAlignment="Center" Stretch="Uniform" Stroke="{StaticResource TextSecondary}" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M10.5,18 A7.5,7.5 0 1 1 18,10.5 M16.2,16.2 L22,22"/>
                            <Grid Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="搜索应用、命令或路径" Foreground="{StaticResource TextHint}" FontSize="17" IsHitTestVisible="False" FontFamily="Segoe UI Variable Display, Segoe UI, Microsoft YaHei UI">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBox x:Name="SearchBox" Style="{StaticResource SearchBoxStyle}" Text="{Binding QueryText, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="30" Height="24" Margin="0,0,8,0" CornerRadius="8" BorderBrush="#16FFFFFF" BorderThickness="1" Background="#10FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                                    <Ellipse Width="12" Height="12" StrokeThickness="2" Stroke="{StaticResource AccentBrush}" StrokeDashArray="3,2" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                        <Ellipse.RenderTransform><RotateTransform x:Name="LoadingRotate"/></Ellipse.RenderTransform>
                                        <Ellipse.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetName="LoadingRotate" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:0.9"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Ellipse.Triggers>
                                    </Ellipse>
                                </Border>
                                <TextBlock Text="唤起" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="{StaticResource TextSecondary}" FontSize="13" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                <Border Style="{StaticResource KeycapStyle}">
                                    <TextBlock Text="Alt+Space" Foreground="{StaticResource TextPrimary}" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border x:Name="ResultsContainer" Grid.Row="1" Style="{StaticResource ResultsContainerStyle}" ClipToBounds="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Margin="18,12,18,6" Text="结果" Foreground="{StaticResource TextHint}" FontSize="12" FontWeight="SemiBold" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasItems, ElementName=ResultList}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Grid Grid.Row="1">
                                <ListBox x:Name="ResultList" Margin="0,0,0,10" ItemsSource="{Binding Results}" SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden" VirtualizingPanel.IsVirtualizing="True" MaxHeight="440">
                                    <ListBox.Style>
                                        <Style TargetType="ListBox">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <Trigger Property="HasItems" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListBox.Style>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Width="30" Height="30" Margin="0,0,12,0" CornerRadius="9" Background="#10FFFFFF" BorderBrush="#10FFFFFF" BorderThickness="1" VerticalAlignment="Center">
                                                    <Image Source="{Binding Icon, Converter={StaticResource IconFallback}}" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                </Border>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Title}" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                                    <TextBlock Text="{Binding Subtitle}" Margin="0,3,0,0" Foreground="{StaticResource TextSecondary}" FontSize="12" TextTrimming="CharacterEllipsis" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                                </StackPanel>
                                                <Border Grid.Column="2" Style="{StaticResource TypeTagStyle}" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Tag}" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                                </Border>
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                            <Border Grid.Row="2" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,1,0,0" Background="#660D0E11">
                                <Grid Margin="18,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="启动器" VerticalAlignment="Center" Foreground="{StaticResource TextHint}" FontSize="12" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Style="{StaticResource KeycapStyle}"><TextBlock Text="↑↓" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                        <TextBlock Text="选择" Margin="0,0,14,0" VerticalAlignment="Center" Foreground="{StaticResource TextHint}" FontSize="12" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                        <Border Style="{StaticResource KeycapStyle}"><TextBlock Text="Enter" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                        <TextBlock Text="打开" Margin="0,0,14,0" VerticalAlignment="Center" Foreground="{StaticResource TextHint}" FontSize="12" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                        <Border Style="{StaticResource KeycapStyle}"><TextBlock Text="Esc" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                        <TextBlock Text="关闭" VerticalAlignment="Center" Foreground="{StaticResource TextHint}" FontSize="12" FontFamily="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
